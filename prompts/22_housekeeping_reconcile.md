# Prompt 22 — Housekeeping & reconciliation (Pillar 6)

> **DRAFT skeleton (2026-06-16).** Pays down documentation + state debt so the
> repo is honest while the other pillars are in flight. Read end-to-end, execute
> in phase order, leave a `prompts/_session_log.md` entry. Deviations →
> `prompts/_HIL.md`. Touches only `docs/` and `prompts/` — **zero code-path
> conflict**, so it runs in Wave 1 alongside everything. Buildable in one session.

## Mission

Close four pieces of debt:

1. Write the two missing docs that the README + Definition-of-done already
   reference: `docs/limitations.md` and `docs/scaling.md`.
2. Reconcile `docs/roadmap.md` with `docs/strategy.md` (and the new
   `docs/operationalization.md`) so no doc contradicts another.
3. **Close the stale `prompts/_HIL.md` FLAG A** — its premise is now false.
4. Restate (do not resolve) the open FLAG B cleanly.

**Scope guard.** Documentation + state hygiene only. No code, no schema, no score
change. No new claims that aren't already reproducible from a repo script (the
fact-checking checklist applies).

## Phase 1 — `docs/limitations.md`

The honest scope-boundaries doc (README + close-out plan both promise it). Pull
from `docs/operationalization.md` §2 and the validation report; cover at least:

- L1 — thin validation signal (N=5 in the pilot; cite `scripts/11_validate.py`
  via `<!-- generated by -->`) and how pillar 2 addresses it.
- L2 — structural-only score / dropped FWI (cite the v0.2.0 changelog) and how
  pillar 0 addresses it.
- AOI-relative scores (methodology §11) — not comparable across AOIs.
- Fuel-map scale mismatch (validation report's plan-caveat #3).
- Burn-scar domain shift + the WU-10 detection-not-forecast framing.
- rank-not-probability + demonstrator-not-service (#6, #9).

Every numeric claim must be reproducible from a named script (fact-checking
checklist). No new numbers invented.

## Phase 2 — `docs/scaling.md`

The production-path notes the methodology stubs-list flags. Cover: the documented
PostGIS path (README "Stack" already names it for asset-volume growth), the
national reference-distribution requirement for cross-AOI comparability
(strategy.md §7(6); blocks absolute thresholds — see pillar 3), R2 hosting for
large geodata (already in place — `wildfire.cheias.pt`), and the honest statement
that scaling beyond the pilot is *documented*, not *done*. No production claims (#9).

## Phase 3 — Reconcile roadmap ↔ strategy ↔ operationalization

- `docs/roadmap.md` is dated 2026-06-09 (pre-WU-9, pre-six-pillars). Update its
  "Where it's going" to point at `docs/operationalization.md` as the live program,
  and remove/repair any claim that contradicts it.
- The specific contradiction to fix: strategy.md §7 lists network/topology as
  "P2 — later, higher effort"; operationalization.md promotes it to a Wave-1 WU.
  Add a one-line note in strategy.md §7 ("superseded by docs/operationalization.md
  §3 — network/topology is now a first-class WU") rather than rewriting the brief.
- Ensure no doc still implies the network pillar is simultaneously "future P2" and
  "shipped".

## Phase 4 — Close `prompts/_HIL.md` FLAG A; restate FLAG B

> **Verified finding (2026-06-16):** FLAG A's premise "no live artefact has been
> touched" is **false now**. The de-grid p85 burn-scar run **`20260615T192025Z`**
> is published: `stac/burn-scar-recent/burn-scar-20260615T192025Z/` is the live
> STAC item, and `docs/app/data/style_data.json` points the geobrowser at
> `burn_scar_3857_20260615T192025Z.tif`; the R2 upload was handed to the human in
> the 2026-06-15 "WU-10 burn-scar PUBLISH" session-log entry.

- Re-verify before editing: confirm the live run-id in the STAC item dir and in
  `style_data.json` (do not take this prompt's word for it — re-check the tree).
- Update FLAG A in `prompts/_HIL.md` (append the resolution; **do not delete** the
  original — it is the audit trail): mark the **display COG + STAC publish DONE**
  (with the run-id), and make the remaining-open part explicit — the **exposure
  re-score + `docs/validation_report.md` refresh** were part of FLAG A and have
  **not** happened. Then either:
  - **(a)** confirm the exposure parquet does not depend on the new COG in the
    backdated validation window (`recent_burn_share_12mo` is nulled there — verify
    in `validation.py` / the report) and **close FLAG A** as fully resolved; or
  - **(b)** if a forward-looking (non-backdated) re-score is wanted, spin it as an
    explicit follow-on WU and leave FLAG A pointing at that — but do **not** leave
    it asserting "no live artefact touched".
  Make the (a)/(b) call explicit; if it needs the human, that itself is the flag.
- FLAG B (NDVI/land-cover guard in `features.py`): leave **open + deferred**,
  restated cleanly. Not in this program's scope.

## Tests / gates

No code changes expected. Run the four gates anyway to prove nothing regressed:

```bash
uv run ruff check . && uv run ruff format --check . \
  && uv run pyright src tests scripts && uv run pytest
```

If a doc touches the STAC catalog wording, also:

```bash
uv run stac-validator stac/catalog.json --recursive
```

## Done-when

- `docs/limitations.md` + `docs/scaling.md` committed, every number reproducible
  from a named script, no production claims.
- roadmap ↔ strategy ↔ operationalization reconciled; the network-pillar
  contradiction removed.
- `_HIL.md` FLAG A updated with the true current state (publish DONE; re-score
  open) and either closed (a) or pointed at a follow-on (b); FLAG B restated open.
- Four gates green; session-log entry summarising the four phases.

## HIL flags

- **FLAG (public surface):** `limitations.md`, `scaling.md`, and the roadmap edits
  are public docs — human approval before they ship.
- **FLAG (state):** the FLAG-A (a)-vs-(b) call (close vs follow-on re-score) is a
  data-contract decision — surface it to the human if there is any doubt the
  backdated parquet is independent of the new COG.
